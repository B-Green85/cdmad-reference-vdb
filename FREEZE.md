# Freeze: v0.1-invariant-frozen

This repository is frozen at tag `v0.1-invariant-frozen`.

Invariants guaranteed at this tag:
- Dockerized Postgres + pgvector runs
- FastAPI boots and responds to /health
- Canonical CDMAD chunks are seeded
- Semantic query endpoint returns results
- Proof pack collected via scripts/proof_pack.sh

Any future work must occur on a new branch.
