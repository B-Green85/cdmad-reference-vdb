[project]
name = "cdmad-reference-vdb"
version = "0.1.0"
description = "CDMAD Reference Vector Database API"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.34,<1",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
